---
const { Content, frontmatter } = Astro.props;
const sectionId = frontmatter.slug || frontmatter.title.toLowerCase().replace(/\s+/g, '-');
---
<section id={sectionId} class="section text-gray-400">
	<h2 class="text-3xl font-bold text-white mb-4">{frontmatter.title}</h2>
  <Content />
</section>